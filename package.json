{
  "name": "getexp-framework",
  "version": "0.1.0",
  "private": true,
  "description": "GetExp — opinionated development framework (Golden Path) for building APIs, web and mobile apps fast.",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "biome check .",
    "format": "biome format --write .",
    "check": "biome check . && pnpm -r typecheck && pnpm -r test",
    "create-app": "node tools/create-app.mjs"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.16",
    "typescript": "5.9.3"
  }
}

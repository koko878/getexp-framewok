# expenses-web

GetExp Golden Path web app (Next.js, App Router).

```bash
pnpm install
pnpm --filter expenses-web dev      # http://localhost:3000
```

Share validation schemas and HTTP/error helpers with your backend via
`@getexp/core`. See `CLAUDE.md` and `docs/conventions/typescript.md`.

# marketplace-mobile

GetExp Golden Path mobile app (Expo / React Native).

```bash
pnpm install
pnpm --filter marketplace-mobile start     # opens Expo dev tools
```

Reuse Zod schemas and `@getexp/core` helpers shared with your backend. See
`CLAUDE.md` and `docs/conventions/typescript.md`.
